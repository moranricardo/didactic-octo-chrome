name: El Toroide Adiamantado (Evolución 818 - Persistente)

on:
  push:
    branches: [ main ]
  workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Necesario para hacer el git push automático
    
    env:
      NODE_ENV: production
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Instalación Ligera
        run: npm ci --prefer-offline

      - name: 📡 Ejecutar Motor de Red Ra Pulse
        run: node index.js

      - name: 💾 Persistir Estado en Repositorio
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add state.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore: actualizar estado de auditoría" && git push)
